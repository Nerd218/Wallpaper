import { StyleSheet, View } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    view: {
        height: 200,
        width: 250,
    },
    textStyle: {
        marginTop: 20,
        marginBottom: 20,

    }
})